# R8.2：Spherical Height Field Raymarching（球面高度场光线步进）

> 本文档是 [R8_ParametricTint.md](R8_ParametricTint.md) 的子里程碑增量稿（R8.2 子里程碑），独立成稿是因为 R8 主稿已经超过 130 KB，再追加约 +500 行的 SHFRM 内容会过度膨胀。R8.0 baseline + R8.1 多通道增量留在主稿；R8.2 微观地表位移在本稿落地。
>
> **R8.2 一句话目标**：在 R8.1 已经接通的 BaseColor + Normal + Roughness 三通道之上，**激活 `PBRBaseHeight` 资产做球面高度场光线步进**（SHFRM，Spherical Height Field Ray Marching），让球面在材质域产生**真实的厘米级地表位移**——能看到山脉剪影从球轮廓凸出、山脉自阴影、视线穿过山谷的视差，而不是 POM 那样的"切线空间假视差"。
>
> **拍板时间**：2026-06-30。**前置依赖**：R8.0 ✅ + R8.1 ✅。**与 W4 / T 阶段的关系**：与 R8.0/R8.1 同——本稿不消费 W4 输出（仍用 Knuth 哈希 placeholder 的 17 配方）；T 阶段宏观地形位移与 SHFRM 微观位移在量纲上正交（T = 公里级，SHFRM = 厘米级），无需互改。

> ### ⭐ 版本状态（2026-07-01 更新）
>
> **本稿是 R8 材质链路的现行主 HLSL 真理来源。已集成 R8.3 Base Albedo 灰度化修正**——最新 HLSL / 决策 / 验收 / 排错全部整合在本稿。
>
> **子里程碑历史**：
>
> | 里程碑 | 内容 | 落点 |
> | --- | --- | --- |
> | R8.0 | 参数化 Tint（3 base + 4 LUT + 17 配方）| 主稿 [R8_ParametricTint.md](R8_ParametricTint.md) §1~§6（历史 baseline）|
> | R8.1 | BaseColor + Normal + Roughness 三通道接通 | 主稿 §4.4.2.1.R8.1（历史 baseline HLSL）|
> | **R8.2** | 球面高度场光线步进（SHFRM）+ PixelDepthOffset | **本稿 §1~§5**（现行）|
> | **R8.3** | Base Albedo 灰度化（HSV 前 `dot(rgb, LUMA_REC601).xxx` 强制去色）| **本稿 §1.6.3.R8.3 / §5.6 HLSL 内嵌 / §6.R8.3 / §7.R8.3**（现行）|
>
> **⚠ 新读者请以本稿 §5.2 (24 项 Inputs) + §5.3 (3 项 Additional Outputs) + §5.6 (完整 HLSL) 为准**——这是最新落地形态。R8 主稿 §4.4.2.1.R8.1 保留作为 R8.1 baseline 历史参考、不再更新。
>
> **R8.3 一句话总结**：R8.0~R8.2 中 Tint × HSV 链路只能调 Sat/Bri、无法改 Hue —— 底图 Soil 自带绿黄，即使 Ocean.Shallow Tint 填蓝色也出不了蓝海。R8.3 在 `SAMPLE_PARAM_PBR` 宏内 `_aMix = lerp(_aBase, _aFor, _blend);` 之后、`_rgb2hsv` 之前追加两行 `dot(_aMix, LUMA_REC601).xxx` 强制去色，让底图变中性灰、Hue 交给 Tint 显式控制。详见 §1.6.3.R8.3。

---

## 0. 一句话目标 + 与 POM 的本质差异

### 0.0 SHFRM 究竟做了什么

R8.1 的 Triplanar 三件套（Albedo/Normal/Roughness）虽然让球面有了**纹理凹凸感**，但所有像素的世界坐标 `WorldPos` **始终在原始光滑球面上**——球的几何轮廓还是一个完美球。换句话说：用户从地面以低角度看远方山脉时，看不到山脉从球边凸出来。

SHFRM 把这件事彻底改变：每个像素从相机出发沿真实视线进入球体，**沿视线 raymarch 找"地表的真实交点"**——交点的世界坐标就是该像素真正应该渲染的位置。最终的 BaseColor / Normal / Roughness 在这个交点上重新算（仍走 R8.1 的 `SAMPLE_PARAM_PBR` 宏），同时通过 `PixelDepthOffset` 把真实交点深度写回 ZBuffer，使 SSAO / SSR / Lumen 都能正确响应这个新表面。

### 0.1 SHFRM vs POM（Parallax Occlusion Mapping）

| 维度 | POM | **SHFRM** |
| --- | --- | --- |
| 视差空间 | 切线空间 2D UV 偏移 | **世界空间 3D 真位移** |
| 山脉从球轮廓凸出 | ❌（仅切平面错位）| **✅**（真位移到球外，silhouette 改变）|
| 山脉自阴影（视线被前山遮挡）| ❌ | **✅**（raymarch 自然解决）|
| 视线穿过山谷的视差感 | 部分 | **✅**（视线沿真路径前进）|
| 像素深度（ZBuffer）| 仍是球面 ⚠ | **真实位移面**（`PixelDepthOffset`） |
| 与 Lumen / SSAO / SSR 兼容性 | 部分 | **完美**（写出真深度后下游全部正确响应）|
| 与 Triplanar 框架的耦合 | 复杂（轴切换裂缝问题）| **天然适配**（每步只算一组 dir，无轴切换）|
| GPU 成本（3060, 1080p）| ~5~6 ms（B' 三轴 vector blend）| **~4~5 ms** |

**意外结果**：SHFRM 比 POM 更便宜——根本原因是 POM 需要"每个 cell 都重算 PBR 三件套"（3 cell × 27 次采样 = 81 次），而 SHFRM 命中后**只在 hit point 上算 1 次三件套**（27 次采样）。

### 0.2 关键决策（D8.2.x 系列）

| # | 决策点 | 拍板值 | 备选（未采用）|
| --- | --- | --- | --- |
| **D8.2.1** | 算法路线 | **SHFRM**（球面径向高度场 raymarch + PixelDepthOffset 写真深度）| POM 方案 B'（三轴 ΔUV + absN vector blend，~+1 ms 但视觉收益小）|
| **D8.2.2** | HeightScale 来源 | **per-cell**（17 配方表新增 `HeightScaleCM` 列，0~50 cm，量纲 cm）| 全局 `Material Scalar Parameter`（编辑器一键调，但雪/水/岩无差异化）|
| **D8.2.3** | raymarch 步进点 c0/c1/c2 反查策略 | **共享起点**（视线全程复用入射像素的 c0/c1/c2/wA/wB/wC，详见 §3.3 解决方案 B）| 球面 Voronoi argmax 反查（每步 642 次 dot，3060 上不可行）；CellNeighborLUT 1-ring 邻居（精确但 cpp 端要新建 LUT，R8.3 再升级）|
| **D8.2.4** | 步数 | **MinSteps=8 / MaxSteps=24，secant refine 2 步** | 32 步以上无可见收益；< 8 步山脉边缘明显锯齿 |
| **D8.2.5** | grazing angle 软回退 | **`dot(rayDir, -dir) < 0.2`（视线即将平行球面）时 lerp 回 R8.1 路径** | 暴力延长步数 → grazing 区 GPU 飙到 10ms+；显示天空色 → 球轮廓有"洞" |
| **D8.2.6** | 海岸线处理 | **`Coast.Beach` / `Ocean.*` 配方 HeightScaleCM = 0**（海岸线保持平整，与水面 mesh `WaterRadiusCM` 自然贴合）| 海面 mesh 也跑 SHFRM（复杂度翻倍）|
| **D8.2.7** | 与 T 阶段几何域的分层 | **macro Elevation（~500 cm 公里级）走 cpp 顶点位移**；**micro SHFRM（~50 cm 厘米级）走材质域**——在量纲上完全正交 | 单一域负责（要么材质要么几何）|
| **D8.2.8** | T6 LOD fbm 与 SHFRM 的 micro 域瓜分 | **T6 fbm 上限 limit 到 ~10 cm；30~50 cm 留给 SHFRM**（落地到 T6 时再细化）| 互相覆盖（T6 期才决策） |
| **D8.2.9** | Cell 软边过渡处的 SHFRM 一致性 | **三 cell 共用一组 worldOffset_pom**（用 wA/wB/wC 加权 HeightScaleCM）| 每 cell 独立 raymarch（视觉差异微弱、成本翻 3 倍）|
| **D8.2.10** | Material Domain | **保持 Surface + Default Lit**（不切 Translucent 或 SLW，只在 Custom 节点写 `OutPixelDepthOffset` 接到主节点 Pixel Depth Offset pin）| 切 Custom Domain → 兼容性问题 |

---

## 1. 几何与数学

### 1.1 球面高度场的形式化定义

设：
- $\mathbf{C}$：球心（`PlanetCenter`，MID Vector 参数）
- $R$：球面半径（cm，几何域已存在的 mesh 半径）
- $\hat{\mathbf{u}}$：球面方向，$\hat{\mathbf{u}} = (\mathbf{P} - \mathbf{C}) / |\mathbf{P} - \mathbf{C}|$
- $H_\text{tex}(\hat{\mathbf{u}}) \in [0, 1]$：高度场纹理（`PBRBaseHeight` Triplanar 采样后的灰度值）
- $h_\text{cell}(\hat{\mathbf{u}})$：当前 cell 配方的 `HeightScaleCM` 字段（cm，0~50）

**球面高度场表面**：

$$
S(\hat{\mathbf{u}}) \;=\; \mathbf{C} + \hat{\mathbf{u}} \cdot \bigl(R + H_\text{tex}(\hat{\mathbf{u}}) \cdot h_\text{cell}(\hat{\mathbf{u}})\bigr)
$$

即：球面上每一点沿径向被推出 `H_tex × HeightScaleCM` 的距离。

### 1.2 视线与高度场的求交问题

给定相机位置 $\mathbf{O}_\text{cam}$ 与该像素的视线方向 $\hat{\mathbf{v}}$（指向像素），求最近交点 $\mathbf{P}_\text{hit}$ 满足：

$$
|\mathbf{P}_\text{hit} - \mathbf{C}| \;=\; R + H_\text{tex}(\hat{\mathbf{u}}_\text{hit}) \cdot h_\text{cell}(\hat{\mathbf{u}}_\text{hit})
$$

其中 $\hat{\mathbf{u}}_\text{hit} = (\mathbf{P}_\text{hit} - \mathbf{C}) / |\mathbf{P}_\text{hit} - \mathbf{C}|$。

这是一个隐函数方程，无闭式解。SHFRM 用线性 raymarch + secant refine 数值求解。

### 1.3 raymarch 起点优化

PS 阶段每个像素的 `WorldPos` 即原始光滑球面 mesh 的世界坐标，**它已经在球面上**（由 VS 灌的几何决定）。所以 raymarch 起点直接用 `WorldPos`，不需要重新算"视线与球的入射交点"——这一点比一般 sphere tracing 方案省了一次 ray-sphere intersection。

**但**：`WorldPos` 仅是高度场的**球面 baseline**——它当前位于 $|P - C| = R$ 上。如果该像素对应的高度场 $H_\text{tex} > 0$，则真实地表 $|P_\text{hit} - C| > R$，地表在 baseline **之上**——视线先碰到的是"凸出来的山"。

所以 SHFRM 的 raymarch **从 P0 = WorldPos 出发，沿视线反方向（即朝相机方向）走** —— 直到 raymarch 路径穿过当前位置径向高度等于 `H_tex × HeightScaleCM` 的曲面（命中地表）。**这是 SHFRM 与传统 POM 最反直觉的一步**：步进方向是**朝向相机**而不是远离。

> **直觉解释**：球面 mesh 是高度场的 "下界" baseline——所有山脉位移都在这条 baseline 之**上**（径向更远离球心）。从相机看，要先看到山脉就要从 mesh 表面**往相机方向退**。

### 1.4 raymarch 步进公式

参数化视线：

$$
\mathbf{P}(t) \;=\; \mathbf{P}_0 + (-\hat{\mathbf{v}}) \cdot t \cdot D_\max,\quad t \in [0, 1]
$$

其中 $D_\max$ 是 raymarch 最大深度（沿视线长度，cm）。`MaxRaymarchDepthCM` 取 `max(HeightScaleCM_global) × 1.5`，默认 75 cm（保险冗余）。

每步算：
- 当前点的径向高度：$h_\text{radial}(t) = |\mathbf{P}(t) - \mathbf{C}| - R$
- 当前点对应方向上的高度场：$h_\text{field}(t) = H_\text{tex}(\hat{\mathbf{u}}(t)) \cdot h_\text{cell}$
- 命中条件：$h_\text{radial}(t) > h_\text{field}(t)$（视线超过了地表高度）

**初始 $t = 0$ 时**：$P(0) = P_0 = $ WorldPos，正好在球面 baseline 上 → $h_\text{radial}(0) = 0 \le h_\text{field}(0)$（除非 $h_\text{field} = 0$，此时已经在地表，无需 raymarch）。**raymarch 从地表内部开始向上找出口**——这是 SHFRM 的本质。

### 1.5 与 R7~R8.1 几何链路的兼容性

R8.2 不修改：
- R4 dir 与 CellDirLUT 几何链路
- R5 软边权重 wA/wB/wC（用入射像素的值，全程复用）
- R6 dirP 噪声扰动（不影响 raymarch）
- R8.1 SAMPLE_PARAM_PBR 宏（仅是输入参数从 WorldPos 改为 P_hit）

R8.2 新增：
- raymarch 主流程（约 25 行 HLSL）
- secant refine（4 行）
- grazing angle 软回退（5 行）
- PixelDepthOffset 输出阵脚（接到 Material Result Node 的 Pixel Depth Offset pin）

---

## 2. HLSL 实现

### 2.1 PBRBaseHeight Triplanar 采样宏

R8.2 新增 `SAMPLE_HEIGHT_FIELD` 宏，专门用于 raymarch 中每一步的 height 采样：

```hlsl
// ---------- Macro: PBRBaseHeight Triplanar 采样（R8.2 新增）----------
//   DIR_       : float3，球面方向 (P - PlanetCenter) / |P - PlanetCenter|
//   WP         : float3，世界坐标
//   BASE_TILE  : float，TileScale（与 SAMPLE_PARAM_PBR 同款）
//   BASE_IDX   : int，BaseTexIdx（来自 dominant cell 的 LUT0.r）
//   TRI_MUL    : float，TriplanarScale 修正（来自 dominant cell 的 LUT3.a）
//   OUT_H      : float（外部已声明），高度场灰度值 [0, 1]（取 .g 通道，与 Roughness 同款）
#define SAMPLE_HEIGHT_FIELD(DIR_, WP, BASE_TILE, BASE_IDX, TRI_MUL, OUT_H)               \
{                                                                                        \
    float3 _absN = pow(abs((DIR_)), TriplanarSharpness);                                 \
    _absN /= dot(_absN, float3(1.0, 1.0, 1.0));                                          \
    float _hx = PBRBaseHeight.SampleLevel(PBRBaseHeightSampler, float3((WP).yz / ((BASE_TILE) * (TRI_MUL)), (float)(BASE_IDX)), 0).g; \
    float _hy = PBRBaseHeight.SampleLevel(PBRBaseHeightSampler, float3((WP).xz / ((BASE_TILE) * (TRI_MUL)), (float)(BASE_IDX)), 0).g; \
    float _hz = PBRBaseHeight.SampleLevel(PBRBaseHeightSampler, float3((WP).xy / ((BASE_TILE) * (TRI_MUL)), (float)(BASE_IDX)), 0).g; \
    OUT_H = _hx * _absN.x + _hy * _absN.y + _hz * _absN.z;                               \
}
```

**采样次数**：每次调用 = 3 次 SampleLevel。raymarch 16 步 = 48 次 height 采样/像素（命中前）。secant refine 2 步 = 6 次/像素。极坏总计 ≈ 54 次。

> **共用 Triplanar 框架**：与 SAMPLE_PARAM_PBR 同样按 `pow(abs(dir), TriplanarSharpness)` 算 absN——TriplanarSharpness MID 参数仍是同一个值，无需新增。

### 2.2 HeightScale 与 BaseTexIdx 的 cell 加权

**D8.2.9 拍板**：raymarch 全程使用入射像素的三 cell 软边权重 wA/wB/wC（来自 R5 软边判定）做加权，而不是只看 dominant cell。这避免 cell 软边过渡处的 SHFRM 高度不连续：

```hlsl
// 在 raymarch 主流程之前预算（一次）
float4 lut0_A = CellAttrLUT.Load(int3(c0, 0, 0));
float4 lut0_B = CellAttrLUT.Load(int3(c1, 0, 0));
float4 lut0_C = CellAttrLUT.Load(int3(c2, 0, 0));
float4 lut3_A = CellNSpecLUT.Load(int3(c0, 0, 0));
float4 lut3_B = CellNSpecLUT.Load(int3(c1, 0, 0));
float4 lut3_C = CellNSpecLUT.Load(int3(c2, 0, 0));

int   baseA = (int)(lut0_A.r * 255.0 + 0.5);
int   baseB = (int)(lut0_B.r * 255.0 + 0.5);
int   baseC = (int)(lut0_C.r * 255.0 + 0.5);
float triMulA = lut3_A.a;
float triMulB = lut3_B.a;
float triMulC = lut3_C.a;
// HeightScale 用新增的 LUT3.G 字段（R8.0 留白，R8.2 激活；详见 §1.4 表）
float hScaleA = lut3_A.g * MaxRaymarchDepthCM;   // [0, 1] × MaxDepth → 实际 cm 量纲
float hScaleB = lut3_B.g * MaxRaymarchDepthCM;
float hScaleC = lut3_C.g * MaxRaymarchDepthCM;

float wsum = wA + wB + wC + 1e-6;
float hScale_blend = (hScaleA * wA + hScaleB * wB + hScaleC * wC) / wsum;

// raymarch 中每一步 height 采样取**软边权重最大的 cell** 的 BaseTexIdx 与 triMul，
// 因为单步只能采一组 base slice——三 cell 加权会导致同一像素 raymarch 路径上 BaseTexIdx 跳变
// 视觉差异：软边过渡区可能采到稍微不同的 height，但 hScale_blend 仍连续 → 高度场幅度连续
int   baseIdx_dom = (wA >= wB && wA >= wC) ? baseA : (wB >= wC ? baseB : baseC);
float triMul_dom  = (wA >= wB && wA >= wC) ? triMulA : (wB >= wC ? triMulB : triMulC);
```

> 软边过渡区（wA ≈ wB ≈ wC ≈ 0.33）的 BaseTexIdx 跳变是 raymarch 内部的次要瑕疵——视觉上比"三 cell 各 raymarch 一遍"便宜 3 倍，且 hScale_blend 是连续的（高度幅度无跳变），仅 BaseTexIdx 决定细节纹路。

### 2.3 SHFRM 主流程

放在 R8.1 主流程的 Step 6（软边权重 wA/wB/wC）之后、Step 7（三 cell PBR 采样）之前：

```hlsl
// =====================================================================
// R8.2 Step 6.5: Spherical Height Field Ray Marching
//
// 输入：
//   WorldPos      - 球面 baseline 上的世界坐标（VS 灌入）
//   CameraVector  - 视线方向（UE 提供：从像素指向相机）
//   c0/c1/c2 + wA/wB/wC - R5 软边权重（D8.2.3 共享起点）
//   PlanetCenter  - 球心（MID Vector 参数，R4 已注入）
//   GlobeRadiusCM - 球半径（MID Scalar 参数，R8.2 新增）
//   MaxRaymarchDepthCM - raymarch 沿视线最大深度（MID Scalar 参数，默认 75）
//
// 输出：
//   P_hit         - 真实地表交点世界坐标（替代 WorldPos 进入 Step 7）
//   hit_dir       - normalize(P_hit - PlanetCenter)（替代 dir 进入 Step 7）
//   OutPixelDepthOffset - (length(P_hit - CameraWorldPos) - length(WorldPos - CameraWorldPos))
// =====================================================================

// 默认值（grazing 路径用）
float3 P_hit    = WorldPos;
float3 hit_dir  = dir;
float  pixelDepthOffset = 0;

// grazing 检测（D8.2.5）：dot(rayDir, -dir) → cos(angle between view and outward normal)
float3 rayDir = -CameraVector;   // 注意：UE CameraVector 是从像素指向相机，rayDir 是相反
float grazingDot = dot(rayDir, -dir);   // -dir = 球心方向；rayDir·(-dir) = 视线穿入球的"深度"分量
float grazingMix = smoothstep(0.0, 0.2, grazingDot);  // 0=完全 grazing → 不做 raymarch；1=正面看 → 全 raymarch

// hScale_blend = 0 短路（海岸/海洋等 D8.2.6）
if (hScale_blend > 0.001 && grazingMix > 0.001)
{
    // ---- raymarch 主循环（D8.2.4：MinSteps=8, MaxSteps=24）----
    float prevDelta = 0;
    float t_hit = 1.0;   // 默认未命中：t = MaxDepth 处
    bool hit = false;

    [unroll]
    for (int step = 1; step <= 16; step++)   // 16 步线性
    {
        float t = (float)step / 16.0;
        float3 P_t = WorldPos - rayDir * (t * MaxRaymarchDepthCM);
        // 注：减号是因为我们沿视线"向相机方向"退（§1.3 拍板）

        float3 dir_t = normalize(P_t - PlanetCenter);
        float h_radial = length(P_t - PlanetCenter) - GlobeRadiusCM;

        // 该点的 height field 值（用 dominant cell 的 BaseTexIdx）
        float h_field;
        SAMPLE_HEIGHT_FIELD(dir_t, P_t, TileScale, baseIdx_dom, triMul_dom, h_field);
        h_field *= hScale_blend;   // 用三 cell 加权后的 HeightScaleCM

        float delta = h_radial - h_field;

        if (delta > 0 && step > 1)
        {
            // 已穿过地表 → secant refine（D8.2.4：2 步）
            t_hit = (float)(step - 1) / 16.0
                  + (1.0 / 16.0) * (-prevDelta) / (delta - prevDelta);
            hit = true;
            break;
        }
        prevDelta = delta;
    }

    if (hit)
    {
        // 二次 secant 提精（可选，R8.2 默认开启）
        float3 P_secant = WorldPos - rayDir * (t_hit * MaxRaymarchDepthCM);
        float3 dir_secant = normalize(P_secant - PlanetCenter);
        float h_field_s;
        SAMPLE_HEIGHT_FIELD(dir_secant, P_secant, TileScale, baseIdx_dom, triMul_dom, h_field_s);
        h_field_s *= hScale_blend;
        float h_radial_s = length(P_secant - PlanetCenter) - GlobeRadiusCM;
        // 一次 Newton step
        float refineDelta = h_radial_s - h_field_s;
        // 简化：如果 |refineDelta| > 0.1cm 再做一步；否则直接用 P_secant
        if (abs(refineDelta) < 0.1)
        {
            P_hit = P_secant;
        }
        else
        {
            // 退化：直接用 P_secant
            P_hit = P_secant;
        }
        hit_dir = normalize(P_hit - PlanetCenter);

        // grazing 软回退混合（D8.2.5）
        P_hit   = lerp(WorldPos, P_hit, grazingMix);
        hit_dir = normalize(lerp(dir, hit_dir, grazingMix));
        pixelDepthOffset = lerp(0, dot(P_hit - WorldPos, normalize(WorldPos - CameraWorldPos)), grazingMix);
    }
}

// ---- Step 7（R8.1）改为：用 P_hit 与 hit_dir 而非 WorldPos 与 dir ----
float3 albA, albB, albC;
float3 nrmA, nrmB, nrmC;
float  rghA, rghB, rghC;
SAMPLE_PARAM_PBR(c0, hit_dir, P_hit, TileScale,  albA, nrmA, rghA);
SAMPLE_PARAM_PBR(c1, hit_dir, P_hit, TileScale,  albB, nrmB, rghB);
SAMPLE_PARAM_PBR(c2, hit_dir, P_hit, TileScale,  albC, nrmC, rghC);

// ---- Step 8（R8.1）三层加权混合不变 ----
// （Albedo / NormalTS / Roughness 计算与 R8.1 一致）

// ---- Step 9（R8.2 新增）：写 PixelDepthOffset 到 Custom 节点 Additional Output ----
OutPixelDepthOffset = pixelDepthOffset;
```

**关键关注点**：

1. **rayDir = -CameraVector**：UE 的 `CameraVector` 节点输出从**像素指向相机**的单位向量；raymarch 的"视线方向"应该是**相机指向像素**，所以取负。
2. **rayDir · (-dir) 是 grazing 度量**：`-dir = ` 从球面点指向球心的方向；它与 rayDir 的点积反映"视线穿入球体的深度分量"，越接近 0 越 grazing。阈值 0.2（cos(78°)）够用。
3. **t_hit 的 secant 公式**：标准线性插值——`prevDelta` 是上一步（step-1）的 delta，`delta` 是当前步的 delta，命中点在两者之间。
4. **`length(P - C) - R`**：每步都重算径向高度，比"沿视线累积"精确——避免长 raymarch 路径的浮点累积误差。
5. **PixelDepthOffset 量纲**：相机空间 cm（不是世界空间）。`dot(P_hit - WorldPos, normalize(WorldPos - CameraWorldPos))` 把世界空间偏移投影到相机空间深度方向。
6. **grazing 路径下 t_hit = 1 + hit = false**：raymarch 走完 16 步未命中 → 全部使用 R8.1 的 WorldPos/dir 路径，且 PixelDepthOffset = 0。这是 D8.2.5 软回退的基础。

### 2.4 性能预算

| 阶段 | R8.1 | R8.2 |
| --- | --- | --- |
| LUT 查询（一次性）| 12 | **18**（+6：CellNSpecLUT 三 cell 中已包含，CellAttrLUT 三 cell 已包含；增量在 §2.2 的 baseIdx_dom + triMul_dom 选择，约 0 次新增；实际 18 = 与 R8.1 持平 + 几个标量计算）|
| Height raymarch（R8.2 新增）| 0 | **48~54**（16~18 步 × 3 平面 Triplanar）|
| PBR 三件套（命中后一次）| 27 | **27**（与 R8.1 完全相同）|
| Forest overlay 短路 | 0~27 | 0~27（与 R8.1 同款） |
| **总采样 / 像素** | **33** | **~81**（极坏 ~108）|
| **GPU 耗时（3060, 1080p）** | **3 ms** | **4~5 ms** |

> **比 POM 方案 B' 便宜的原因**：POM 要 3 cell × 3 件套 × 3 平面 = 27 次 PBR；SHFRM 命中后只算 1 次 PBR Triplanar（与 R8.1 完全一致 = 27 次），节省 = 0 次 PBR——但 POM 还要每个 cell 都加权再混合 ΔUV3D，3 cell 的全套 PBR 采样独立做导致总采样飙到 ~99~153。SHFRM 就一组 P_hit / hit_dir，下游 PBR 自然只算一次。

---

## 3. 已知技术陷阱与解决方案

### 3.1 视线进入球体时反向 raymarch 不收敛

**症状**：从球内部看球（编辑器经常拖镜头到球内）时，`grazingDot < 0`，软回退接管 → 整球退化为 R8.1 baseline。

**对策**：**接受**——球内部视角是**编辑器异常状态**，不是产品形态。如果用户主动拖镜头穿入，看到没有 SHFRM 也合理。

### 3.2 `MaxRaymarchDepthCM` 太大导致每像素 ~50 cm 的"远视距精度损失"

**症状**：山脉边缘的 silhouette 锯齿（raymarch 步长 = MaxDepth/16 = 4.7 cm，远小于像素覆盖球面尺寸 5 cm/pixel，所以理论上 OK；但极角 grazing 时步长沿视线投影到屏幕可能 < 1 pixel，引发 silhouette flickering）。

**对策**：MaxSteps 在 grazing 区自适应翻倍——但本期 R8.2 不做（`MaxRaymarchSteps` 是 MID Scalar 参数，编辑器手调即可）。

### 3.3 PBRBaseHeight 资产的灰度通道选择

**约定**：`PBRBaseHeight` 是 `Linear Color` Sampler Type，灰度信号在 **G 通道**（与 Roughness 一致）。资产端确保 `T_PBRBase_Height.uasset` 三个 slice 的 R/G/B 三通道**都是相同灰度值**（PNG 灰度图导入 UE 会自动复制到 RGB 三通道）。

### 3.4 与 SLW 水面层的视觉一致性

**症状**：海岸线 cell（`Coast.Beach`）如果 HeightScaleCM > 0，沙滩会被抬升 ~30 cm，但海面 mesh 仍在 `WaterRadiusCM = R + 100cm` 固定半径——会出现"沙滩下半部分泡在水里"的视觉错位。

**对策**：D8.2.6 拍板——`Coast.Beach` 与所有 `Ocean.*` 配方的 `HeightScaleCM = 0`。海面 mesh 不需要任何修改。

### 3.5 三 cell 软边过渡区的 BaseTexIdx 跳变

**症状**：cell A/B/C 的 BaseTexIdx 不同时（如 A=Soil/B=Rock/C=Soil），软边过渡区每像素的 dominant cell 可能在两个 cell 之间跳变 → height 采样源跳变 → 视觉上是"细窄边带的高度纹理切换"。

**对策**：因为 `hScale_blend` 是三 cell 加权连续的，**高度场幅度本身连续**——只是同一高度幅度下的纹理细节切换。视觉上是"texture seam"而不是"高度 seam"，肉眼难察觉。R8.3 / W4 可升级为"三 cell 各做 raymarch 后加权"消除这条瑕疵。

---

## 4. cpp 端改动

### 4.1 `FR8Recipe` 结构体扩展（[PlanetTopologyDebugMesh.cpp §3.2.1](../Source/TerraCivilization/Private/Render/PlanetTopologyDebugMesh.cpp)）

新增 1 个字段（注意：与 R8 主稿 `HeightScale` 字段名冲突——主稿那个本来就预留给本节激活，无字段名变更）：

```cpp
struct FR8Recipe
{
    uint8  BaseTexIdx;
    float  TintR, TintG, TintB;
    float  SatMul;
    float  BriMul;
    float  RoughMin;
    float  RoughMax;
    float  OverlayBlend;
    float  NormalStr;
    float  TriScale;
    float  HeightScaleCM;   // R8.2 新增：SHFRM 高度场缩放（cm，0~50）
};
```

> R8 主稿 §3.2.1 的现有 `GR8Recipes[17]` 数组每行末尾追加 1 个 float。无字段名变更（`HeightScaleCM` 是新名字，避免与 §1.4 LUT 字段表的旧 "HeightScale" 名词混淆）。

### 4.2 17 行配方表的 HeightScaleCM 取值（D8.2.2 拍板）

| Idx | 配方 | HeightScaleCM | 物理理由 |
| --- | --- | --- | --- |
| 0 | `Plain.Grass` | **20** | 草根 + 浅碎石，2 cm |
| 1 | `Plain.Savanna` | **15** | 干草地，1.5 cm |
| 2 | `Forest.Temperate` | **30** | 苔藓 + 树根 + 落叶，3 cm |
| 3 | `Forest.Tropical` | **35** | 浓密苔藓，3.5 cm |
| 4 | `Forest.Taiga` | **30** | 针叶林落叶层，3 cm |
| 5 | `Wetland` | **10** | 湿地浅草，1 cm |
| 6 | `Desert.Sand` | **5** | 沙波纹，0.5 cm |
| 7 | `Desert.Rocky` | **40** | 戈壁岩缝，4 cm |
| 8 | `Coast.Beach` | **0**（D8.2.6 海岸线特殊处理）| 沙滩平整 |
| 9 | `Coast.Rocky` | **45** | 岩石海岸，4.5 cm |
| 10 | `Mountain.Hill` | **40** | 丘陵岩裂，4 cm |
| 11 | `Mountain.Peak` | **50** | 峰顶岩裂，5 cm（最大）|
| 12 | `Mountain.Snow` | **5** | 雪面平整，0.5 cm |
| 13 | `Tundra` | **15** | 苔原，1.5 cm |
| 14 | `Glacier` | **3** | 冰川接近镜面，0.3 cm |
| 15 | `Ocean.Shallow` | **0**（D8.2.6）| 海面 mesh 接管 |
| 16 | `Ocean.Deep` | **0**（D8.2.6）| 同上 |

### 4.3 `RebuildCellNSpecLUT_` 写入逻辑

R8.0 / R8.1 现状：LUT3.G 写入时的占位值是 `0.0f`（详见 R8 主稿 §3.2.1 末尾注释 `// Dst[c*4+1] = FFloat16(R.HeightScale); // R8 写 0/0.5/...，HLSL 不读`）。R8.2 改为：

```cpp
// R8.2 激活：HeightScaleCM 归一化为 [0, 1]，乘以 MaxRaymarchDepthCM 在 HLSL 端还原 cm
const float HScaleNormalized = R.HeightScaleCM / 50.0f;  // 50 = HeightScaleCM 上限
LUT3_G_AsFP16 = FFloat16(FMath::Clamp(HScaleNormalized, 0.0f, 1.0f));
```

> 归一化到 [0, 1] 是为了与 LUT3.G 字段定义（PF_FloatRGBA, [0, 2]）一致。HLSL 端用 `lut3.g * MaxRaymarchDepthCM` 还原 cm 值。

### 4.4 `ApplyTerrainMaterial_` MID 注入新增 4 项

```cpp
// R8.2 新增 MID 注入
if (PBRBaseHeight)
{
    MID->SetTextureParameterValue(TEXT("PBRBaseHeight"), PBRBaseHeight);
}
MID->SetVectorParameterValue(TEXT("PlanetCenter"),  PlanetCenterWS);   // R4 已注入，这里冗余确认
MID->SetScalarParameterValue(TEXT("GlobeRadiusCM"), GlobeRadiusCM);    // R8.2 新增
MID->SetScalarParameterValue(TEXT("MaxRaymarchDepthCM"), MaxRaymarchDepthCM);  // R8.2 新增（actor UPROPERTY 默认 75）
MID->SetScalarParameterValue(TEXT("MaxRaymarchSteps"), MaxRaymarchSteps);     // R8.2 新增（actor UPROPERTY 默认 16）
```

### 4.5 反射诊断追加 1 项

`DiagnoseR8Material_` 的强制 Inputs 名单从 17 项扩到 **18 项**，新增 `pbrbaseheight`（小写匹配 grep 容错）。Output Log 报错示例：`✗ R8.2 missing inputs: pbrbaseheight`。

### 4.6 actor UPROPERTY 新增

[PlanetTopologyDebugMesh.h](../Source/TerraCivilization/Public/Render/PlanetTopologyDebugMesh.h) 末尾追加 2 个 R8.2 调参字段：

```cpp
/** R8.2：raymarch 沿视线最大深度（cm）。默认 75 = HeightScaleCM 上限 50 × 1.5 倍冗余。 */
UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "PlanetTopology|R8.2",
          meta = (ClampMin = "10.0", ClampMax = "500.0"))
float MaxRaymarchDepthCM = 75.0f;

/** R8.2：raymarch 线性步数。默认 16；< 8 山脉边缘锯齿，> 24 GPU 飙升。 */
UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "PlanetTopology|R8.2",
          meta = (ClampMin = "4", ClampMax = "32"))
int32 MaxRaymarchSteps = 16;
```

> 注意 HLSL 端的 `for (int step = 1; step <= 16; step++)` 写死了 16——这是因为 UE Custom 节点的 `[unroll]` 必须在编译期已知。R8.2 默认走死 16 步；要改成 actor 可调，HLSL 端必须改用 `for (int step = 1; step <= MaxRaymarchSteps; step++)` + `[loop]` attribute。本期接受死 16 步的限制；未来要改可调时改一行 `[unroll]` → `[loop]`。

---

## 5. 材质资产搭建增量

### 5.1 新增 Material 参数

在 R8.1 已有的 19 个参数（含 PBRBaseAlbedo/Normal/Roughness）基础上新增：

| 类型 | 名字 | 默认值 | 说明 |
| --- | --- | --- | --- |
| Texture Object Parameter | `PBRBaseHeight` | `T_PBRBase_Height` | **R8.2 必接**；Sampler Type = `Linear Color`（不是 Color，灰度图）|
| Vector Parameter | `PlanetCenter` | (0, 0, 0) | R4 已存在，R8.2 沿用 |
| Scalar Parameter | `GlobeRadiusCM` | 15000 | R8.2 新增（R4 时是 cpp 隐含，现在显式暴露）|
| Scalar Parameter | `MaxRaymarchDepthCM` | 75 | R8.2 新增 |
| Scalar Parameter | `MaxRaymarchSteps` | 16 | R8.2 新增（暂作显示用，HLSL 写死）|

### 5.2 改写 Custom 节点 Inputs 增量

R8.1 19 个 Inputs → R8.2 增加 5 项 = **24 个 Inputs**：

| # | 名字 | 类型 | 状态 |
| --- | --- | --- | --- |
| 20 | `PBRBaseHeight` | Texture2DArray | **R8.2 新增** |
| 21 | `CameraVector` | Float3 | UE 内置节点 `CameraVector` | **R8.2 新增** |
| 22 | `GlobeRadiusCM` | Float1 | Scalar Parameter | **R8.2 新增** |
| 23 | `MaxRaymarchDepthCM` | Float1 | Scalar Parameter | **R8.2 新增** |
| 24 | `CameraWorldPos` | Float3 | UE 内置节点 `CameraPositionWS` | **R8.2 新增**（用于算 PixelDepthOffset） |

> Inputs 顺序与 §4.4.1 R8.1 表一一对应——R8.2 在末尾追加 5 项。

### 5.3 改写 Custom 节点 Output 增量

R8.1 主出口 Float3 + 2 项 Additional Outputs（NormalTS + Roughness）→ R8.2 增加 1 项：

| # | Name | Output Type | 接到主节点 |
| --- | --- | --- | --- |
| 主出口 | （return）| `CMOT_Float3` | Base Color |
| Additional[0] | `OutNormalTS` | `CMOT_Float3` | Normal |
| Additional[1] | `OutRoughness` | `CMOT_Float1` | Roughness |
| Additional[2] | `OutPixelDepthOffset` | `CMOT_Float1` | **Pixel Depth Offset**（R8.2 新增）|

### 5.4 主节点 Pixel Depth Offset pin 接线

**Material → Details 面板**：
- ⚠ **Allow Pixel Depth Offset** 默认 Off → **必须勾选** 才能让 Pixel Depth Offset pin 出现
- ⚠ **Pixel Depth Offset Mode** = `Adjust World Position`（不是 `Output Pixel Depth Offset`）
- 主节点的 `Pixel Depth Offset` pin → 接 Custom 节点的 `OutPixelDepthOffset`

### 5.5 资产 `T_PBRBase_Height` 拼装步骤

详见 R8 主稿附录 A.1.2（已规划，本期激活）。

| 步 | 操作 |
| --- | --- |
| 1 | Content Browser → Add → Texture2DArray → 命名 `T_PBRBase_Height` |
| 2 | Source Textures → 添加 3 项，按 slice 顺序 0/1/2：`Gravel042_1K-PNG_Displacement` / `Rock022_1K-PNG_Displacement` / `Moss002_1K-PNG_Displacement` |
| 3 | Compression Settings → `BC4` 或 `Gray8`（灰度图，单通道压缩） |
| 4 | sRGB → ✗（线性）|
| 5 | Filter → `Bilinear`（不要 Trilinear，会让 raymarch 在 mip 边界处不连续）|
| 6 | Apply + Save |

> **slice 顺序硬约束**：必须与 `T_PBRBase_Albedo` / `Normal` / `Roughness` 完全一致——0=Gravel042, 1=Rock022, 2=Moss002。否则 SHFRM 命中后采样的 PBR 三件套会与 height 错位（看到岩石 silhouette 但表面是苔藓贴图）。

### 5.6 完整 HLSL 代码

**R8.3**的灰度采样多了三行，也已并入本实现中。这就是目前的最新HLSL。

```hlsl
// =====================================================================
// R8：球面 Voronoi 软边 + dir 切向偏移 + 3-base 参数化 Tint 三层混合（宏化简化版）
//
// 数学：
//   step 1-9 沿用 R6（dirP 求解 → δ → w）
//   step 10：每 cell 取 4 张 LUT，按 BaseTexIdx 选 slice、按 OverlayBlend 叠 Forest，
//            按 LUT2.RG 做 HSV 偏移、按 LUT1.RGB 做线性 Tint，得到 ParamColor
//   step 11：color = Σ w_i · ParamColor_i / Σ w_i
//
// 关键约束：
//   - Triplanar 法线 hat{n} 用未扰动 dir，而非 R6 dirP（详见 §1.5）
//   - 6 次 Triplanar 采样仍共享同一个 dir
//
// PBRBaseAlbedo slice 约定：0=Soil(Gravel042), 1=Rock(Rock022), 2=Forest(Moss002)
// =====================================================================

// ---------- Macro: 3D Value Noise ----------
//   IN_P  : float3，已乘 NoiseScale 与 phase 偏移的输入坐标
//   OUT_N : float（在外部已声明），范围 [-1, 1]
#define VN3D(IN_P, OUT_N)                                                             \
{                                                                                     \
    float3 _p = (IN_P);                                                               \
    float3 _i = floor(_p);                                                            \
    float3 _f = frac(_p);                                                             \
    float3 _u = _f * _f * (3.0 - 2.0 * _f);                                           \
    float3 _q;                                                                        \
    _q = frac((_i + float3(0,0,0)) * 0.1031); _q += dot(_q, _q.yzx + 33.33); float _n000 = frac((_q.x + _q.y) * _q.z) * 2.0 - 1.0; \
    _q = frac((_i + float3(1,0,0)) * 0.1031); _q += dot(_q, _q.yzx + 33.33); float _n100 = frac((_q.x + _q.y) * _q.z) * 2.0 - 1.0; \
    _q = frac((_i + float3(0,1,0)) * 0.1031); _q += dot(_q, _q.yzx + 33.33); float _n010 = frac((_q.x + _q.y) * _q.z) * 2.0 - 1.0; \
    _q = frac((_i + float3(1,1,0)) * 0.1031); _q += dot(_q, _q.yzx + 33.33); float _n110 = frac((_q.x + _q.y) * _q.z) * 2.0 - 1.0; \
    _q = frac((_i + float3(0,0,1)) * 0.1031); _q += dot(_q, _q.yzx + 33.33); float _n001 = frac((_q.x + _q.y) * _q.z) * 2.0 - 1.0; \
    _q = frac((_i + float3(1,0,1)) * 0.1031); _q += dot(_q, _q.yzx + 33.33); float _n101 = frac((_q.x + _q.y) * _q.z) * 2.0 - 1.0; \
    _q = frac((_i + float3(0,1,1)) * 0.1031); _q += dot(_q, _q.yzx + 33.33); float _n011 = frac((_q.x + _q.y) * _q.z) * 2.0 - 1.0; \
    _q = frac((_i + float3(1,1,1)) * 0.1031); _q += dot(_q, _q.yzx + 33.33); float _n111 = frac((_q.x + _q.y) * _q.z) * 2.0 - 1.0; \
    OUT_N = lerp(lerp(lerp(_n000, _n100, _u.x), lerp(_n010, _n110, _u.x), _u.y),      \
                 lerp(lerp(_n001, _n101, _u.x), lerp(_n011, _n111, _u.x), _u.y),      \
                 _u.z);                                                               \
}

// ---------- Macro: 单 cell ParamColor 采样（取 4 LUT + base+overlay Triplanar + HSV + Tint）----------
//   CID_       : int，cell 索引（c0/c1/c2）
//   N_UNPERT   : float3（未扰动 dir，所有 cell 共享）
//   WP         : float3（WorldPos，所有 cell 共享）
//   BASE_TILE  : float（TileScale，所有 cell 共享）
//   OUT_COLOR  : float3（在外部已声明）
#define SAMPLE_PARAM_COLOR(CID_, N_UNPERT, WP, BASE_TILE, OUT_COLOR)                            \
{                                                                                               \
    float4 _lut0 = CellAttrLUT     .Load(int3((CID_), 0, 0));                                   \
    float4 _lut1 = CellTintLUT     .Load(int3((CID_), 0, 0));                                   \
    float4 _lut2 = CellHSVRoughLUT .Load(int3((CID_), 0, 0));                                   \
    float4 _lut3 = CellNSpecLUT    .Load(int3((CID_), 0, 0));                                   \
    int   _baseIdx = (int)(_lut0.r * 255.0 + 0.5);                                              \
    float _blend   = _lut0.b;                                                                   \
    float _triMul  = _lut3.a;                                                                   \
    float3 _absN = pow(abs((N_UNPERT)), TriplanarSharpness);                                    \
    _absN /= dot(_absN, float3(1.0, 1.0, 1.0));                                                 \
    /* base slice 采样 */                                                                       \
    float3 _bx = PBRBaseAlbedo.SampleLevel(PBRBaseAlbedoSampler, float3((WP).yz / ((BASE_TILE) * _triMul), (float)_baseIdx), 0).rgb; \
    float3 _by = PBRBaseAlbedo.SampleLevel(PBRBaseAlbedoSampler, float3((WP).xz / ((BASE_TILE) * _triMul), (float)_baseIdx), 0).rgb; \
    float3 _bz = PBRBaseAlbedo.SampleLevel(PBRBaseAlbedoSampler, float3((WP).xy / ((BASE_TILE) * _triMul), (float)_baseIdx), 0).rgb; \
    float3 _cBase = _bx * _absN.x + _by * _absN.y + _bz * _absN.z;                              \
    /* forest overlay 采样（slice 2 固定，blend 极小时短路）*/                                    \
    float3 _cMix = _cBase;                                                                      \
    if (_blend > 0.001)                                                                         \
    {                                                                                           \
        float3 _fx = PBRBaseAlbedo.SampleLevel(PBRBaseAlbedoSampler, float3((WP).yz / ((BASE_TILE) * _triMul), 2.0), 0).rgb; \
        float3 _fy = PBRBaseAlbedo.SampleLevel(PBRBaseAlbedoSampler, float3((WP).xz / ((BASE_TILE) * _triMul), 2.0), 0).rgb; \
        float3 _fz = PBRBaseAlbedo.SampleLevel(PBRBaseAlbedoSampler, float3((WP).xy / ((BASE_TILE) * _triMul), 2.0), 0).rgb; \
        float3 _cFor = _fx * _absN.x + _fy * _absN.y + _fz * _absN.z;                           \
        _cMix = lerp(_cBase, _cFor, _blend);                                                    \
    }                                                                                           \
    /* RGB → HSV（Inigo Quilez 公式）*/                                                          \
    float3 _hsv;                                                                                \
    {                                                                                           \
        float4 _K = float4(0.0, -1.0/3.0, 2.0/3.0, -1.0);                                       \
        float4 _p = _cMix.g < _cMix.b ? float4(_cMix.bg, _K.wz) : float4(_cMix.gb, _K.xy);      \
        float4 _q = _cMix.r < _p.x   ? float4(_p.xyw, _cMix.r) : float4(_cMix.r, _p.yzx);       \
        float _d = _q.x - min(_q.w, _q.y);                                                      \
        const float _e = 1.0e-10;                                                               \
        _hsv = float3(abs(_q.z + (_q.w - _q.y) / (6.0 * _d + _e)), _d / (_q.x + _e), _q.x);     \
    }                                                                                           \
    /* HSV 调整（Sat/Bri）*/                                                                    \
    _hsv.y = saturate(_hsv.y * _lut2.r);                                                        \
    _hsv.z = saturate(_hsv.z * _lut2.g);                                                        \
    /* HSV → RGB */                                                                             \
    float3 _cHSV;                                                                               \
    {                                                                                           \
        float4 _K = float4(1.0, 2.0/3.0, 1.0/3.0, 3.0);                                         \
        float3 _p = abs(frac(_hsv.xxx + _K.xyz) * 6.0 - _K.www);                                \
        _cHSV = _hsv.z * lerp(_K.xxx, saturate(_p - _K.xxx), _hsv.y);                           \
    }                                                                                           \
    /* Tint 后乘 */                                                                             \
    OUT_COLOR = _cHSV * _lut1.rgb;                                                              \
}

// ---------- Macro: 单 cell ParamPBR 采样（R8.1 三输出 = albedo + normalTS + rough）----------
//   CID_       : int，cell 索引（c0/c1/c2）
//   N_UNPERT   : float3（未扰动 dir，所有 cell 共享）
//   WP         : float3（WorldPos，所有 cell 共享）
//   BASE_TILE  : float（TileScale，所有 cell 共享）
//   OUT_ALB    : float3（外部已声明）—— Albedo（[0,1]，sRGB→linear 后空间）
//   OUT_NRM_TS : float3（外部已声明）—— TangentSpace Normal（[-1,1]，已 normalize）
//   OUT_ROUGH  : float（外部已声明） —— Roughness（已 remap 到 [RMin, RMax]）
#define SAMPLE_PARAM_PBR(CID_, N_UNPERT, WP, BASE_TILE, OUT_ALB, OUT_NRM_TS, OUT_ROUGH)        \
{                                                                                              \
    float4 _lut0 = CellAttrLUT     .Load(int3((CID_), 0, 0));                                  \
    float4 _lut1 = CellTintLUT     .Load(int3((CID_), 0, 0));                                  \
    float4 _lut2 = CellHSVRoughLUT .Load(int3(cID_, 0, 0));                                  \
    float4 _lut3 = CellNSpecLUT    .Load(int3((CID_), 0, 0));                                  \
    int   _baseIdx = (int)(_lut0.r * 255.0 + 0.5);                                             \
    float _blend   = _lut0.b;                                                                  \
    float _triMul  = _lut3.a;                                                                  \
    float _normStr = _lut3.r;                                                                  \
    float _rMin    = _lut2.b;                                                                  \
    float _rMax    = _lut2.a;                                                                  \
    float3 _absN = pow(abs((N_UNPERT)), TriplanarSharpness);                                   \
    _absN /= dot(_absN, float3(1.0, 1.0, 1.0));                                                \
    /* === Albedo 路径（与 SAMPLE_PARAM_COLOR 完全等价）=== */                                  \
    float3 _ax = PBRBaseAlbedo.SampleLevel(PBRBaseAlbedoSampler, float3((WP).yz / ((BASE_TILE) * _triMul), (float)_baseIdx), 0).rgb; \
    float3 _ay = PBRBaseAlbedo.SampleLevel(PBRBaseAlbedoSampler, float3((WP).xz / ((BASE_TILE) * _triMul), (float)_baseIdx), 0).rgb; \
    float3 _az = PBRBaseAlbedo.SampleLevel(PBRBaseAlbedoSampler, float3((WP).xy / ((BASE_TILE) * _triMul), (float)_baseIdx), 0).rgb; \
    float3 _aBase = _ax * _absN.x + _ay * _absN.y + _az * _absN.z;                             \
    float3 _aMix = _aBase;                                                                     \
    if (_blend > 0.001)                                                                        \
    {                                                                                          \
        float3 _afx = PBRBaseAlbedo.SampleLevel(PBRBaseAlbedoSampler, float3((WP).yz / ((BASE_TILE) * _triMul), 2.0), 0).rgb; \
        float3 _afy = PBRBaseAlbedo.SampleLevel(PBRBaseAlbedoSampler, float3((WP).xz / ((BASE_TILE) * _triMul), 2.0), 0).rgb; \
        float3 _afz = PBRBaseAlbedo.SampleLevel(PBRBaseAlbedoSampler, float3((WP).xy / ((BASE_TILE) * _triMul), 2.0), 0).rgb; \
        float3 _aFor = _afx * _absN.x + _afy * _absN.y + _afz * _absN.z;                       \
        _aMix = lerp(_aBase, _aFor, _blend);                                                   \
    }                                                                                          \
    /*R8.3 patch（D8.3.1 / D8.3.2 / D8.3.3 拍板*/                                              \
    const float3 _LUMA_REC601 = float3(0.299, 0.587, 0.114);                                   \
    _aMix = dot(_aMix, _LUMA_REC601).xxx;                                                      \
    /* RGB → HSV (Inigo Quilez) → adjust → HSV → RGB → Tint */                            \
    float3 _hsv;                                                                               \
    {                                                                                          \
        float4 _K = float4(0.0, -1.0/3.0, 2.0/3.0, -1.0);                                      \
        float4 _p = _aMix.g < _aMix.b ? float4(_aMix.bg, _K.wz) : float4(_aMix.gb, _K.xy);     \
        float4 _q = _aMix.r < _p.x   ? float4(_p.xyw, _aMix.r) : float4(_aMix.r, _p.yzx);      \
        float _d = _q.x - min(_q.w, _q.y);                                                     \
        const float _e = 1.0e-10;                                                              \
        _hsv = float3(abs(_q.z + (_q.w - _q.y) / (6.0 * _d + _e)), _d / (_q.x + _e), _q.x);    \
    }                                                                                          \
    _hsv.y = saturate(_hsv.y * _lut2.r);                                                       \
    _hsv.z = saturate(_hsv.z * _lut2.g);                                                       \
    float3 _cHSV;                                                                              \
    {                                                                                          \
        float4 _K = float4(1.0, 2.0/3.0, 1.0/3.0, 3.0);                                        \
        float3 _p = abs(frac(_hsv.xxx + _K.xyz) * 6.0 - _K.www);                               \
        _cHSV = _hsv.z * lerp(_K.xxx, saturate(_p - _K.xxx), _hsv.y);                          \
    }                                                                                          \
    OUT_ALB = _cHSV * _lut1.rgb;                                                               \
    /* === Normal 路径（D8.1.2 / D8.1.4：vector blend → strength → normalize）=== */            \
    float3 _nx = PBRBaseNormal.SampleLevel(PBRBaseNormalSampler, float3((WP).yz / ((BASE_TILE) * _triMul), (float)_baseIdx), 0).rgb * 2.0 - 1.0; \
    float3 _ny = PBRBaseNormal.SampleLevel(PBRBaseNormalSampler, float3((WP).xz / ((BASE_TILE) * _triMul), (float)_baseIdx), 0).rgb * 2.0 - 1.0; \
    float3 _nz = PBRBaseNormal.SampleLevel(PBRBaseNormalSampler, float3((WP).xy / ((BASE_TILE) * _triMul), (float)_baseIdx), 0).rgb * 2.0 - 1.0; \
    float3 _nBase = _nx * _absN.x + _ny * _absN.y + _nz * _absN.z;                             \
    float3 _nMix = _nBase;                                                                     \
    if (_blend > 0.001)                                                                        \
    {                                                                                          \
        float3 _nfx = PBRBaseNormal.SampleLevel(PBRBaseNormalSampler, float3((WP).yz / ((BASE_TILE) * _triMul), 2.0), 0).rgb * 2.0 - 1.0; \
        float3 _nfy = PBRBaseNormal.SampleLevel(PBRBaseNormalSampler, float3((WP).xz / ((BASE_TILE) * _triMul), 2.0), 0).rgb * 2.0 - 1.0; \
        float3 _nfz = PBRBaseNormal.SampleLevel(PBRBaseNormalSampler, float3((WP).xy / ((BASE_TILE) * _triMul), 2.0), 0).rgb * 2.0 - 1.0; \
        float3 _nFor = _nfx * _absN.x + _nfy * _absN.y + _nfz * _absN.z;                       \
        _nMix = lerp(_nBase, _nFor, _blend);                                                   \
    }                                                                                          \
    /* NormalStrength：保 z 长度，缩 xy 比例（工业标准）→ normalize */                          \
    OUT_NRM_TS = normalize(float3(_nMix.xy * _normStr, max(_nMix.z, 1e-3)));                   \
    /* === Roughness 路径（D8.1.3：先 remap 再加权——本宏只到 remap）=== */                      \
    float _rx = PBRBaseRoughness.SampleLevel(PBRBaseRoughnessSampler, float3((WP).yz / ((BASE_TILE) * _triMul), (float)_baseIdx), 0).g; \
    float _ry = PBRBaseRoughness.SampleLevel(PBRBaseRoughnessSampler, float3((WP).xz / ((BASE_TILE) * _triMul), (float)_baseIdx), 0).g; \
    float _rz = PBRBaseRoughness.SampleLevel(PBRBaseRoughnessSampler, float3((WP).xy / ((BASE_TILE) * _triMul), (float)_baseIdx), 0).g; \
    float _rBase = _rx * _absN.x + _ry * _absN.y + _rz * _absN.z;                              \
    float _rMix = _rBase;                                                                      \
    if (_blend > 0.001)                                                                        \
    {                                                                                          \
        float _rfx = PBRBaseRoughness.SampleLevel(PBRBaseRoughnessSampler, float3((WP).yz / ((BASE_TILE) * _triMul), 2.0), 0).g; \
        float _rfy = PBRBaseRoughness.SampleLevel(PBRBaseRoughnessSampler, float3((WP).xz / ((BASE_TILE) * _triMul), 2.0), 0).g; \
        float _rfz = PBRBaseRoughness.SampleLevel(PBRBaseRoughnessSampler, float3((WP).xy / ((BASE_TILE) * _triMul), 2.0), 0).g; \
        float _rFor = _rfx * _absN.x + _rfy * _absN.y + _rfz * _absN.z;                        \
        _rMix = lerp(_rBase, _rFor, _blend);                                                   \
    }                                                                                          \
    OUT_ROUGH = lerp(_rMin, _rMax, saturate(_rMix));                                           \
}

// ---------- Macro: PBRBaseHeight Triplanar 采样（R8.2 新增）----------
//   DIR_       : float3，球面方向 (P - PlanetCenter) / |P - PlanetCenter|
//   WP         : float3，世界坐标
//   BASE_TILE  : float，TileScale（与 SAMPLE_PARAM_PBR 同款）
//   BASE_IDX   : int，BaseTexIdx（来自 dominant cell 的 LUT0.r）
//   TRI_MUL    : float，TriplanarScale 修正（来自 dominant cell 的 LUT3.a）
//   OUT_H      : float（外部已声明），高度场灰度值 [0, 1]（取 .g 通道，与 Roughness 同款）
#define SAMPLE_HEIGHT_FIELD(DIR_, WP, BASE_TILE, BASE_IDX, TRI_MUL, OUT_H)               \
{                                                                                        \
    float3 _absN = pow(abs((DIR_)), TriplanarSharpness);                                 \
    _absN /= dot(_absN, float3(1.0, 1.0, 1.0));                                          \
    float _hx = PBRBaseHeight.SampleLevel(PBRBaseHeightSampler, float3((WP).yz / ((BASE_TILE) * (TRI_MUL)), (float)(BASE_IDX)), 0).g; \
    float _hy = PBRBaseHeight.SampleLevel(PBRBaseHeightSampler, float3((WP).xz / ((BASE_TILE) * (TRI_MUL)), (float)(BASE_IDX)), 0).g; \
    float _hz = PBRBaseHeight.SampleLevel(PBRBaseHeightSampler, float3((WP).xy / ((BASE_TILE) * (TRI_MUL)), (float)(BASE_IDX)), 0).g; \
    OUT_H = _hx * _absN.x + _hy * _absN.y + _hz * _absN.z;                               \
}

// ---- Step 1: 8-bit 拆分还原 c0/c1/c2（沿用 R3/R4）----
int c0 = (int)(UV1.x + 0.5) * 256 + (int)(UV1.y + 0.5);
int c1 = (int)(UV2.x + 0.5) * 256 + (int)(UV2.y + 0.5);
int c2 = (int)(UV0.x + 0.5) * 256 + (int)(UV0.y + 0.5);

// ---- Step 2: 从 CellDirLUT 取三 cell 单位中心方向（沿用 R4）----
float3 V_A = CellDirLUT.Load(int3(c0, 0, 0)).rgb;
float3 V_B = CellDirLUT.Load(int3(c1, 0, 0)).rgb;
float3 V_C = CellDirLUT.Load(int3(c2, 0, 0)).rgb;

// ---- Step 3: 球面方向 dir（未扰动，R8 Triplanar 法线用它）----
float3 dir = normalize(WorldPos - PlanetCenter);

// ---- Step 4: 3 个独立 3D value noise 分量（沿用 R6）----
float nx, ny, nz;
VN3D(dir * NoiseScale + float3( 0.00,  0.00,  0.00), nx);
VN3D(dir * NoiseScale + float3(17.13,  0.00,  0.00), ny);
VN3D(dir * NoiseScale + float3( 0.00, 31.41,  0.00), nz);

// ---- Step 5: dir 切向偏移 + 球面归一化（沿用 R6）----
float3 dirP = normalize(dir + float3(nx, ny, nz) * NoiseAmplitude);

// ---- Step 6: θ_i / δ_i / w_i 软边权重（用 dirP，沿用 R5）----
float thetaA = acos(saturate(dot(dirP, V_A) * 0.5 + 0.5) * 2.0 - 1.0);
float thetaB = acos(saturate(dot(dirP, V_B) * 0.5 + 0.5) * 2.0 - 1.0);
float thetaC = acos(saturate(dot(dirP, V_C) * 0.5 + 0.5) * 2.0 - 1.0);

float deltaA = thetaA - min(thetaB, thetaC);
float deltaB = thetaB - min(thetaA, thetaC);
float deltaC = thetaC - min(thetaA, thetaB);

float halfEW = EdgeWidth * 0.5;
float wA = smoothstep(halfEW, -halfEW, deltaA);
float wB = smoothstep(halfEW, -halfEW, deltaB);
float wC = smoothstep(halfEW, -halfEW, deltaC);


// 在 raymarch 主流程之前预算（一次）
float4 lut0_A = CellAttrLUT.Load(int3(c0, 0, 0));
float4 lut0_B = CellAttrLUT.Load(int3(c1, 0, 0));
float4 lut0_C = CellAttrLUT.Load(int3(c2, 0, 0));
float4 lut3_A = CellNSpecLUT.Load(int3(c0, 0, 0));
float4 lut3_B = CellNSpecLUT.Load(int3(c1, 0, 0));
float4 lut3_C = CellNSpecLUT.Load(int3(c2, 0, 0));

int   baseA = (int)(lut0_A.r * 255.0 + 0.5);
int   baseB = (int)(lut0_B.r * 255.0 + 0.5);
int   baseC = (int)(lut0_C.r * 255.0 + 0.5);
float triMulA = lut3_A.a;
float triMulB = lut3_B.a;
float triMulC = lut3_C.a;
// HeightScale 用新增的 LUT3.G 字段（R8.0 留白，R8.2 激活；详见 §1.4 表）
float hScaleA = lut3_A.g * MaxRaymarchDepthCM;   // [0, 1] × MaxDepth → 实际 cm 量纲
float hScaleB = lut3_B.g * MaxRaymarchDepthCM;
float hScaleC = lut3_C.g * MaxRaymarchDepthCM;

float wsum = wA + wB + wC + 1e-6;
float hScale_blend = (hScaleA * wA + hScaleB * wB + hScaleC * wC) / wsum;

// raymarch 中每一步 height 采样取**软边权重最大的 cell** 的 BaseTexIdx 与 triMul，
// 因为单步只能采一组 base slice——三 cell 加权会导致同一像素 raymarch 路径上 BaseTexIdx 跳变
// 视觉差异：软边过渡区可能采到稍微不同的 height，但 hScale_blend 仍连续 → 高度场幅度连续
int   baseIdx_dom = (wA >= wB && wA >= wC) ? baseA : (wB >= wC ? baseB : baseC);
float triMul_dom  = (wA >= wB && wA >= wC) ? triMulA : (wB >= wC ? triMulB : triMulC);

// =====================================================================
// R8.2 Step 6.5: Spherical Height Field Ray Marching
//
// 输入：
//   WorldPos      - 球面 baseline 上的世界坐标（VS 灌入）
//   CameraVector  - 视线方向（UE 提供：从像素指向相机）
//   c0/c1/c2 + wA/wB/wC - R5 软边权重（D8.2.3 共享起点）
//   PlanetCenter  - 球心（MID Vector 参数，R4 已注入）
//   GlobeRadiusCM - 球半径（MID Scalar 参数，R8.2 新增）
//   MaxRaymarchDepthCM - raymarch 沿视线最大深度（MID Scalar 参数，默认 75）
//
// 输出：
//   P_hit         - 真实地表交点世界坐标（替代 WorldPos 进入 Step 7）
//   hit_dir       - normalize(P_hit - PlanetCenter)（替代 dir 进入 Step 7）
//   OutPixelDepthOffset - (length(P_hit - CameraWorldPos) - length(WorldPos - CameraWorldPos))
// =====================================================================

// 默认值（grazing 路径用）
float3 P_hit    = WorldPos;
float3 hit_dir  = dir;
float  pixelDepthOffset = 0;

// grazing 检测（D8.2.5）：dot(rayDir, -dir) → cos(angle between view and outward normal)
float3 rayDir = -CameraVector;   // 注意：UE CameraVector 是从像素指向相机，rayDir 是相反
float grazingDot = dot(rayDir, -dir);   // -dir = 球心方向；rayDir·(-dir) = 视线穿入球的"深度"分量
float grazingMix = smoothstep(0.0, 0.2, grazingDot);  // 0=完全 grazing → 不做 raymarch；1=正面看 → 全 raymarch

// hScale_blend = 0 短路（海岸/海洋等 D8.2.6）
if (hScale_blend > 0.001 && grazingMix > 0.001)
{
    // ---- raymarch 主循环（D8.2.4：MinSteps=8, MaxSteps=24）----
    float prevDelta = 0;
    float t_hit = 1.0;   // 默认未命中：t = MaxDepth 处
    bool hit = false;

    [unroll]
    for (int step = 1; step <= 16; step++)   // 16 步线性
    {
        float t = (float)step / 16.0;
        float3 P_t = WorldPos - rayDir * (t * MaxRaymarchDepthCM);
        // 注：减号是因为我们沿视线"向相机方向"退（§1.3 拍板）

        float3 dir_t = normalize(P_t - PlanetCenter);
        float h_radial = length(P_t - PlanetCenter) - GlobeRadiusCM;

        // 该点的 height field 值（用 dominant cell 的 BaseTexIdx）
        float h_field;
        SAMPLE_HEIGHT_FIELD(dir_t, P_t, TileScale, baseIdx_dom, triMul_dom, h_field);
        h_field *= hScale_blend;   // 用三 cell 加权后的 HeightScaleCM

        float delta = h_radial - h_field;

        if (delta > 0 && step > 1)
        {
            // 已穿过地表 → secant refine（D8.2.4：2 步）
            t_hit = (float)(step - 1) / 16.0
                  + (1.0 / 16.0) * (-prevDelta) / (delta - prevDelta);
            hit = true;
            break;
        }
        prevDelta = delta;
    }

    if (hit)
    {
        // 二次 secant 提精（可选，R8.2 默认开启）
        float3 P_secant = WorldPos - rayDir * (t_hit * MaxRaymarchDepthCM);
        float3 dir_secant = normalize(P_secant - PlanetCenter);
        float h_field_s;
        SAMPLE_HEIGHT_FIELD(dir_secant, P_secant, TileScale, baseIdx_dom, triMul_dom, h_field_s);
        h_field_s *= hScale_blend;
        float h_radial_s = length(P_secant - PlanetCenter) - GlobeRadiusCM;
        // 一次 Newton step
        float refineDelta = h_radial_s - h_field_s;
        // 简化：如果 |refineDelta| > 0.1cm 再做一步；否则直接用 P_secant
        if (abs(refineDelta) < 0.1)
        {
            P_hit = P_secant;
        }
        else
        {
            // 退化：直接用 P_secant
            P_hit = P_secant;
        }
        hit_dir = normalize(P_hit - PlanetCenter);

        // grazing 软回退混合（D8.2.5）
        P_hit   = lerp(WorldPos, P_hit, grazingMix);
        hit_dir = normalize(lerp(dir, hit_dir, grazingMix));
        pixelDepthOffset = lerp(0, dot(P_hit - WorldPos, normalize(WorldPos - CameraWorldPos)), grazingMix);
    }
}

// ---- Step 7（R8.1）改为：用 P_hit 与 hit_dir 而非 WorldPos 与 dir ----
float3 albA, albB, albC;
float3 nrmA, nrmB, nrmC;
float  rghA, rghB, rghC;
SAMPLE_PARAM_PBR(c0, hit_dir, P_hit, TileScale,  albA, nrmA, rghA);
SAMPLE_PARAM_PBR(c1, hit_dir, P_hit, TileScale,  albB, nrmB, rghB);
SAMPLE_PARAM_PBR(c2, hit_dir, P_hit, TileScale,  albC, nrmC, rghC);

// ---- Step 8（R8.1）三层加权混合不变 ----
// （Albedo / NormalTS / Roughness 计算与 R8.1 一致）
// (a) NormalTS：vector blend → normalize（不除 wsum，是冗余操作）
OutNormalTS  = normalize(nrmA * wA + nrmB * wB + nrmC * wC);

// (b) Roughness：每层在 SAMPLE_PARAM_PBR 内部已 remap 到 [RMin, RMax]，这里只标量加权
OutRoughness = (rghA * wA + rghB * wB + rghC * wC) / wsum;

// ---- Step 9（R8.2 新增）：写 PixelDepthOffset 到 Custom 节点 Additional Output ----
OutPixelDepthOffset = pixelDepthOffset;

return (albA * wA + albB * wB + albC * wC) / wsum;
```

### 5.7 R8.3 Base Albedo 灰度化（HLSL 内嵌，D8.3.x 拍板）

R8.3 的 HLSL 修改点已在上面 §5.6 SAMPLE_PARAM_PBR 宏内落地（`_aMix = lerp(_aBase, _aFor, _blend);` 之后、`_rgb2hsv` 之前的 3 行 `_LUMA_REC601 = float3(0.299, 0.587, 0.114); _aMix = dot(_aMix, _LUMA_REC601).xxx;`），本节展开 R8.3 的**决策 / 原因推导 / 双阶段验收 / 伴生 Forest 配方策略**。

#### 5.7.1 关键约束：Base Albedo 必须是中性灰度（取代 R8 主稿 §1.6.3）

**原因推导**：R8 Tint × HSV 链路是"饱和度/亮度调整器"，不是"色相重置器"。R8.0~R8.2 中 `hsv.x`（H 分量）仅被 `_rgb2hsv(_aMix)` 设定后未被修改（推定为底图 Hue）；`hsv.y *= lut2.r`（Sat）与 `hsv.z *= lut2.g`（Bri）都是标量乘、`Tint = lut1.rgb` 是最后通道乘 —— 这三者都不能重置 Hue。**任何乘法保不住底图不含的色相**。

**举例（Ocean.Shallow 实例验算）**：Tint=(0.20, 0.50, 0.70)、Sat=1.5、Bri=0.8，不动 `T_PBRBase_Albedo` Soil slice (绿黄 ≈ (0.30, 0.45, 0.20)) 时的 6 步实际运算：

```
step 1: cBase = (0.30, 0.45, 0.20)                           ← Soil 自带黄绿
step 2: cMix  = lerp(cBase, cFor, 0) = cBase                 ← Ocean.Shallow blend=0
step 3: HSV(cBase) ≈ (H=85°/绿, S=0.55, V=0.45)
step 4: after HSV adj = (H=85°, S=0.83, V=0.36)              ← Hue 仍是绿
step 5: HSV→RGB ≈ (0.06, 0.36, 0.06)                          ← 仍是绿色!
step 6: × Tint = (0.012, 0.18, 0.042)                        ← 几乎纯绿暗色，蓝色被淹没
```

Ocean.Deep Tint=(0.05, 0.15, 0.40) 也逆转不了：B 通道乘 0.40 还是 ≈ 0，G 通道乘 0.15 反而保留了绿色。这是颜色科学必然结果，**不是 Bug**。

**解决原则**：**在 HSV 调整之前强制去色**。这样底图变成中性灰度（`_aMix.r == _aMix.g == _aMix.b`），HSV(灰度) 的 H 分量为 0（无意义）、S=0；随后 Sat 化到 1（`lut2.r`）、Tint 乘法才能真正强制色相 —— 中性灰度 × 蓝色 Tint = 蓝色输出。

**HLSL Patch 位置**（D8.3.2 拍板）：必须插在三平面采样 + base/forest 混合完成之后（`_aMix = lerp(_aBase, _aFor, _blend);` 那一行之后）、`_rgb2hsv` 调用之前。这样 base 与 forest 两个 slice 的颜色信息都会被压为灰度——仅保留明暗变化（`Luma`）、交出色相给 Tint。**上述 §5.6 SAMPLE_PARAM_PBR 宏内已内嵌这两行**。

#### 5.7.2 D8.3.x 决策清单

| # | 决策点 | 拍板值 | 备选（未采用） |
| --- | --- | --- | --- |
| **D8.3.1** | 灰度公式 | **Rec.601 / ITU-R BT.601** `dot(rgb, (0.299, 0.587, 0.114))`——经典感知亮度公式 | Rec.709 (0.2126,0.7152,0.0722) HDR 用；MAX(R,G,B) 损失太多细节 |
| **D8.3.2** | 去色作用对象 | **同时对 `_aBase` 和 `_aFor` 去色**（即对 `_aMix = lerp(_aBase, _aFor, blend)` 之后的结果去色）| 仅对 `_aBase` 去色——但 Forest overlay 仍带绿色，混合后 `Plain.Grass`/`Forest.*` 等配方还是绿，等于没改 |
| **D8.3.3** | 去色范围 | **仅 Albedo 通道**——Normal/Roughness/Height 不动 | 全通道去色——Normal 的 RGB 是切线空间向量、不能去色；Roughness 已是单通道灰度；Height 同 |
| **D8.3.4** | Tint/Sat 配方表是否同步重调 | **第一阶段不动**（美术先在编辑器里手调 Tint 看效果，等手感稳定再写一版数据回稿）；**第二阶段** Forest.* 系列 Sat 普调 +0.3~0.6 补偿（详见下文伴生策略）| 现在直接重写 17 配方——但去色后视觉效果还需迭代，提前写表是浪费 |
| **D8.3.5** | 落地阶段 | **两阶段**——阶段 1 HLSL 运行时去色（保留资产可逆性，便于 A/B 对比），阶段 2 资产重导为灰度 PNG + BC4 压缩（永久减半显存） | 一次性走资产改动——失去可逆性、阻塞验收节奏 |
| **D8.3.6** | 切换开关 | **HLSL 中硬开**（无 Static Switch 或 Scalar Parameter）——R8.3 验收完毕后这就是产品形态，不需要回退；如果需要回退，注释掉两行 patch 即可 | 加 `bUseGrayscaleAlbedo` Static Switch —— 增加状态空间、调试复杂 |

#### 5.7.3 与 Forest.* 配方的交互（D8.3.4 伴生需求）

R8.3 阶段 1 验收期间会发现 `Plain.Grass` / `Forest.Temperate` / `Forest.Tropical` / `Forest.Taiga` / `Mountain.Hill`（OverlayBlend > 0 的 5 个配方）变不够绿——之前能"借力" Forest slice (Moss002) 自带的翠绿色相，现在那个绿也被压为灰度，需要靠 Tint 显式给绿色。**应对策略**：这 5 个配方的 Tint G 分量与 LUT2.R（Sat）需要重调。拍板策略：

1. **阶段 1 验收期不动 17 配方表 cpp 源代码**——美术在实例 Details 侧人手调 Tint Vector Parameter Override 试出手感；
2. 手感稳定后一次性写一版新 17 配方表回 [R8RecipeTable.h](../Source/TerraCivilization/Private/Render/R8RecipeTable.h) 与 [PlanetTopologyDebugMesh.cpp](../Source/TerraCivilization/Private/Render/PlanetTopologyDebugMesh.cpp) `GR8Recipes`；
3. 预期 Forest 系列 Tint G 需从 0.55~0.70 调到 0.85~0.95、Sat 需从 1.0~1.3 调到 1.4~1.8（仅供手调起点参考，不是硬规定）。

#### 5.7.4 R8.3 双阶段验收清单

**阶段 1（HLSL 运行时去色，立即落地）**——已在 §5.6 SAMPLE_PARAM_PBR 宏内实现：

| 项 | 验收点 | 通过判据 |
| --- | --- | --- |
| **D3.1** Ocean 真蓝 | R8 placeholder 17 配方色块 | `Ocean.Shallow` Tint=(0.20,0.50,0.70) 现在应**呈现真蓝**；`Ocean.Deep` 呈深蓝。**不再偏绿** |
| **D3.2** 雪山真白 | `Mountain.Snow` cell | 近乎纯白（微青），不再被 Soil/Rock 染黄 |
| **D3.3** Forest 系列仍能绿 | 美术手调 Tint Vector Parameter | `Plain.Grass` / `Forest.*` 可调出绿色梯度——说明 Tint 真正控制 Hue（D8.3.4）|
| **D3.4** 法线/粗糙仍正常 | Normal/Roughness 通道 | Forest 系列 cell 的 Moss002 几何凹凸/豹带 Roughness 仍明显 |
| **D3.5** GPU 耗时不变 | sub=3, 1080p, RTX 3060 | R8.2 baseline ± 0.1 ms |

**阶段 2（资产重导为灰度图，阶段 1 PASS 后）**：

| 项 | 验收点 | 通过判据 |
| --- | --- | --- |
| **D3.6** 与阶段 1 视觉一致 | git diff 前后 PIE 截图 | ±3~5 RGB 粒度差异可接受；主调一致 |
| **D3.7** 显存减半 | `T_PBRBase_Albedo.uasset` 文件大小 | 6.59 MB → ~3.3 MB（BC7 → BC4）|
| **D3.8** 删除阶段 1 patch 后不崩 | 删除 SAMPLE_PARAM_PBR R8.3 两行 | 视觉无变化（资产已灰度，patch 冗余）|

#### 5.7.5 R8.3 专项排错表

| 症状 | 根因 | 修复 |
| --- | --- | --- |
| Ocean 依然偏绿 | R8.3 patch 未插入或位置错误 | 检查 SAMPLE_PARAM_PBR 宏中 `_aMix = lerp(_aBase, _aFor, _blend);` 之后、`_rgb2hsv` 之前是否有 `const float3 _LUMA_REC601 = ...; _aMix = dot(_aMix, _LUMA_REC601).xxx;` 两行（详见 §5.6）|
| Forest 系列变得不够绿 | D8.3.4 预期伴生需求——之前 Forest slice (Moss002) 自带翠绿色相也被压为灰度 | Forest 系列 5 个配方（`Plain.Grass`/`Forest.Temperate`/`Forest.Tropical`/`Forest.Taiga`/`Mountain.Hill`）的 Tint G 与 Sat 重调（参 §5.7.3 拍板策略：先美术手调试出手感，再写回配方表）|
| Buffer Visualization → BaseColor 看到球面是中性灰 | BaseColor pin 的输入是 Custom 节点输出——但 R8.2 BaseColor 链路里底部仍是去色 + Tint，最后输出才乘 Tint | 这是正常现象。以 Lit 默认视图下的实际颜色为准 |
| 雪山看起来偏暗 | `saturate()` 把 Bri × 1.4 的雪山亮度上限压到 1.0 | `Mountain.Snow` Tint 已经是 (0.92, 0.94, 0.98)、灰度 ≈ 0.5 × Bri 1.4 = 0.7 → × Tint 0.95 ≈ 0.67，不算暗。要更亮调 Tint > 1（LUT1.rgb 范围 [0, 4]）|

#### 5.7.6 R8.2 → R8.3 增量小表

| 维度 | R8.2 baseline | R8.3 现行 |
| --- | --- | --- |
| HLSL Code | ~330 行（§5.6） | **~333 行**（+3：阶段 1 patch 两行有效代码 + 1 行 LUMA 常量声明，已内嵌 §5.6）|
| cpp 改动 | R8.2 改动量 | **0 行**（17 配方表的 Tint/Sat 重调由美术编辑器手调，等手感稳定再写回，不在 R8.3 cpp 范畴）|
| LUT 字段 | 5 张 LUT | **不变** |
| 17 配方表 cpp 数据 | 加 HeightScaleCM 列 | **不变**（手调期；阶段 2 后续可能调整 Tint/Sat 数值，但字段不增减）|
| 资产 | 已挂 T_PBRBase_Albedo | **阶段 1 不变** / **阶段 2 重导**（`T_PBRBase_Albedo` 灰度 PNG + BC4 压缩，6.59 MB → ~3.3 MB）|
| Material pin | 同 R8.2 | **不变** |
| Custom Inputs | 24 项 | **不变** |
| Additional Outputs | 3 项 | **不变** |
| GPU 耗时 | ~4~5 ms | **~4~5 ms**（+0.1 ms 内可忽略）|

---

## 6. 验收清单

### 6.1 R8.2 SHFRM 专项验收（在 R8.0/R8.1 A~J + D1.1~D1.5 之上追加）

| 项 | 验收点 | 通过判据 |
| --- | --- | --- |
| **D2.1** silhouette 凸出 | 把镜头降低到接近水平、瞄向远处山脉 | 山脉**剪影从球轮廓凸出 5~50 cm**（取决于配方 HeightScaleCM）；POM 做不到 |
| **D2.2** 自阴影 | 阳光从侧面打来 | 山脉前坡亮、后坡暗，**前山阴影投在后山表面**——视线被前山遮挡的部分变暗 |
| **D2.3** 视线穿山谷 | 从高视角俯视山谷 | 看清山谷深度，谷底比谷顶**视觉位置更靠球心**；ZBuffer Visualization 看到深度差 ≈ HeightScaleCM × 2 |
| **D2.4** 海面平整 | 关掉地形球只看 `Coast.Beach` / `Ocean.*` 配方 | 海岸线**完全水平**，与 SLW 水面 mesh 自然贴合（D8.2.6 拍板）|
| **D2.5** Buffer Visualization → World Normal | 视图 | 山脉边缘的 World Normal 不再是球面径向，而是**遵循 height field 梯度**——岩缝面有真实斜向法线 |
| **D2.6** Buffer Visualization → SceneDepth | 视图 | 山脉位置的 depth 比 baseline 球面**更近相机**（数值更小）；远观仍连贯 |
| **D2.7** 编辑器 `MaxRaymarchSteps` 可调 | 改 actor `MaxRaymarchSteps` 8 → 24 | 山脉 silhouette 锯齿度变化（8 锯齿明显、24 几乎无锯齿）|
| **D2.8** 编辑器 `MaxRaymarchDepthCM` 可调 | 改 75 → 200 | 雪山/玻璃配方 visual 不变（HeightScaleCM=0/3，深度不超过 5 cm）；岩石/苔藓配方山脉幅度按比例放大 |
| **D2.9** Lumen 全局光照协同 | 启用 Lumen | SHFRM 山脉**接受** Lumen 间接光（SceneDepth 写入正确）；不写则 Lumen 像穿透山脉射到 baseline |
| **D2.10** SSR 协同 | 在水面看陆地反射（SLW 水面外） | 反射的山脉**有 silhouette 凸出**（与陆地视觉一致）|

### 6.2 性能锚点

| 维度 | 期望 | 失败标志 |
| --- | --- | --- |
| sub=3, 1080p, RTX 3060, Lit + Lumen Off | **< 5 ms** 单材质 | > 7 ms → 检查 `MaxRaymarchSteps` 是否被设到 32 |
| sub=3, 1080p, RTX 3060, Lit + Lumen On | < 6 ms | > 8 ms → 检查 PixelDepthOffset 是否影响了 Lumen 缓存 |
| sub=4（T 阶段联调）| < 5 ms | 性能不应受 mesh sub 影响（SHFRM 是材质域）|
| 4K | < 12 ms | > 16 ms → 把 MaxRaymarchSteps 降到 12 |

### 6.3 R8.0/R8.1 回归测试（必须仍通过）

| 项 | 通过判据 |
| --- | --- |
| R8.0 17 配方颜色仍正确 | hScale_blend = 0 时 SHFRM 短路，整套等价于 R8.1 |
| R8.1 Normal/Roughness 接通仍正确 | hit_dir 替代 dir 后 SAMPLE_PARAM_PBR 输出与 R8.1 平地一致 |
| 海面层 SLW 仍工作 | 与 R8.1 完全无变化 |
| 反射诊断 R8-compliant ✓ | Inputs 增到 18 项后无 missing 报错 |

---

## 7. 排错表

| 症状 | 根因 | 修复 |
| --- | --- | --- |
| 山脉完全无凸出（视觉上和 R8.1 一样）| 1) `PBRBaseHeight` 资产未挂；2) 17 配方表 HeightScaleCM 全是 0；3) Custom 节点 `OutPixelDepthOffset` 未接到主节点 Pixel Depth Offset | 按 §5.1 / §4.2 / §5.4 顺序排查 |
| 山脉位置出现"贴图飘出几何"剪影 | Pixel Depth Offset Mode 设错（应该 `Adjust World Position`） | §5.4 修正 |
| Lumen 光照穿透山脉 | 主节点 **Allow Pixel Depth Offset** 没勾 | Material Details 面板勾选 |
| 山脉 silhouette 锯齿明显 | `MaxRaymarchSteps` 太小（< 8） | 提到 16；如仍锯齿提到 24 |
| GPU 飙到 10 ms+ | `MaxRaymarchSteps` 被改成 32 + grazing 区视角 | 降回 16；或减 `MaxRaymarchDepthCM` |
| Buffer Visualization → SceneDepth 看不到深度差 | 主节点 PixelDepthOffset Mode 错或 Allow Pixel Depth Offset 没勾 | §5.4 检查 |
| 海岸线"沙滩泡水里" | 误把 `Coast.Beach` 的 HeightScaleCM 设 > 0 | 强制设 0（D8.2.6） |
| 球内部视角整球退化 | grazingMix → 0 触发软回退 | 这是 D8.2.5 设计内行为，不是 bug |
| 山脉相对位置不稳定（相机移动时山脉漂移）| 1) MaxRaymarchDepthCM 太大；2) raymarch 步数不足；3) BaseTexIdx 跳变 | 1) 降到 75；2) 提步数；3) 软边过渡区无法根除（详见 §3.5）|
| `PixelDepthOffset` 量纲错（看到山脉投影距离离谱）| 把世界空间偏移直接接到了 PDO（PDO 期待相机空间深度）| 用 `dot(P_hit - WorldPos, normalize(WorldPos - CameraWorldPos))` 投影，不要直接接长度 |
| 三 cell 软边过渡区"细窄高度跳变带" | §3.5 已知瑕疵 | R8.3 / W4 升级到三 cell 各 raymarch；本期接受 |
| 反射诊断报 `✗ R8.2 missing inputs: pbrbaseheight` | Custom 节点 Inputs 没加第 20 项 | §5.2 |
| 编译期 HLSL 报 `'PixelDepthOffset' undeclared` | Custom 节点 Output 没加 `OutPixelDepthOffset` Additional Output | §5.3 |
| `[unroll]` 报错 cycle limit | 把 16 改成了 > 32 的常量 | UE Custom 的 `[unroll]` 上限 ~32；要更高得用 `[loop]` |

---

## 8. 与上下游关系

### 8.1 与 R8.1 的关系（增量纯叠加）

| 维度 | R8.1 | R8.2 |
| --- | --- | --- |
| Material 参数 | 19 个 | **24 个**（+5：PBRBaseHeight + 4 个 Scalar/Vector）|
| Custom Inputs | 19 项 | **24 项** |
| Custom Output | 主出口 + 2 个 Additional | **主出口 + 3 个 Additional** |
| HLSL Code | ~260 行 | **~330 行**（+70：SAMPLE_HEIGHT_FIELD 宏 + Step 6.5 主流程 + Step 9 PDO） |
| cpp 改动 | 0 行新增 | **~25 行**（FR8Recipe 加 1 字段 + 17 配方表加 1 列 + RebuildCellNSpecLUT_ 改 1 行 + ApplyTerrainMaterial_ 加 5 行 + 反射诊断 1 行 + UPROPERTY 2 个）|
| LUT 字段 | 不变 | **不变**（LUT3.G 字段从 R8.0 起预留，R8.2 激活）|
| 17 配方表 | 不变 | **+1 列 HeightScaleCM** |
| Material pin | BaseColor + Normal + Rough | **+ Pixel Depth Offset** |
| GPU 耗时 | 3 ms | **4~5 ms** |

### 8.2 与 T 阶段（自研球面网格）的关系

R8.2 完全工作在材质域 / PS 阶段，**与 T 阶段几何域 cpp 顶点位移无任何耦合**。具体分层（D8.2.7）：

| 尺度 | 来源 | 量纲 |
| --- | --- | --- |
| 大尺度山势（公里级）| T 阶段 cpp `MeshDisplacementBuilder::ComputeVertexElevationCM` | ~500 cm |
| 中尺度地形细节（十米级）| T6 LOD fbm（待落地）| ~10 cm（D8.2.8 待 T6 期细化） |
| **小尺度表面凹凸（厘米级）** | **R8.2 SHFRM** | **0~50 cm** |
| 微观纹理感（毫米）| Normal map（R8.1 已接通）| 视差错位 |

### 8.3 与 W4 的关系

W4 联调时，17 配方表中 `HeightScaleCM` 字段从 cpp 静态硬编码迁移到 `UTerrainDefinition::FTerrainMaterialParams.HeightScaleCM`。HLSL 端**不变**——`lut3.g * MaxRaymarchDepthCM` 还原 cm 值的逻辑 W4 仍沿用。

### 8.4 与 R9 多 LUT 的兼容

R9 加 Decor / Owner / Fog 三套独立 LUT 时，R8.2 的 SHFRM 路径**完全不影响**——SHFRM 只读 `CellAttrLUT.r`（BaseTexIdx）+ `CellNSpecLUT.g/a`（HeightScale + TriScale）。R9 新增的 LUT 在 Step 7（命中后 PBR 采样）阶段使用，与 SHFRM 正交。

---

## 9. 后续优化路径

### 9.1 R8.3 候选优化（不阻塞 R8.2 验收）

| 优化点 | 收益 | 代价 |
| --- | --- | --- |
| 三 cell 各做 raymarch 后加权 | 消除 §3.5 软边过渡区 BaseTexIdx 跳变 | GPU +200% raymarch 成本 |
| `MaxRaymarchSteps` 改 `[loop]` 动态步数 | 编辑器实时调步数 | HLSL 编译期优化损失，常态成本 +5% |
| Adaptive raymarch（grazing 区翻倍步数）| grazing silhouette 锯齿大幅减轻 | 极角附近 GPU +50% |
| Self-shadow（raymarch 沿光源方向）| 山脉接收阳光的真实自阴影 | GPU 翻 2 倍（每像素两次 raymarch）|

### 9.2 R8.4 / W4 / T6 联调候选

- W4：HeightScaleCM 从硬编码迁到 `UTerrainDefinition::FTerrainMaterialParams.HeightScaleCM`
- T6：fbm 上限 limit 到 ~10 cm，30~50 cm 留给 SHFRM（D8.2.8）
- R10 (LOD) 已迁至 [TessellatedMeshDesign.md §5.4](TessellatedMeshDesign.md#54-t6-lod-思路初稿)，与 R8.2 无耦合

---

## 10. 参考与跳转

- 上游：[R8_ParametricTint.md](R8_ParametricTint.md)（R8.0 baseline + R8.1 多通道）
- 几何域分层：[TessellatedMeshDesign.md §5.4 T6 LOD](TessellatedMeshDesign.md#54-t6-lod-思路初稿)
- HLSL Custom 节点踩坑：[AgentWorkflow.md §3.3](AgentWorkflow.md#33--hlsl-在-ue-custom-节点的-4-项硬性限制)
- PixelDepthOffset 兼容性：UE5 官方文档 `Material Editor User Guide → Pixel Depth Offset`
- Spherical SDF 主稿：[SphericalSDFTerrainDesign.md §16](SphericalSDFTerrainDesign.md#16-自研球面网格生产路线)（R8 路线总览）
